{
  "enabled": {
    "description": "是否启用关键词自动回复",
    "type": "bool",
    "default": true
  },
  "case_sensitive": {
    "description": "关键词匹配是否区分大小写",
    "type": "bool",
    "default": false,
    "hint": "关闭后英文关键词不区分大小写；中文关键词不受影响。"
  },
  "whole_word": {
    "description": "整词匹配（防止英文/数字关键词被粘连误触发）",
    "type": "bool",
    "default": false,
    "hint": "开启后，含英文/数字的关键词必须独立出现才触发：例如关键词 mj 不再命中 mmj、mja2、mj_1；前后是中文或标点时仍正常触发（如 这是mj、mj吧）。中文关键词不受影响，仍支持句子中间匹配。"
  },
  "send_interval": {
    "description": "多条回复之间的发送间隔（秒）",
    "type": "float",
    "default": 0.5,
    "hint": "命中关键词后需要连续发送多条回复时，两条消息之间的等待时间，用于防止平台风控。设为 0 表示不等待；范围 0-60，超出会被自动收敛。"
  },
  "rules": {
    "description": "关键词回复规则列表",
    "type": "template_list",
    "templates": {
      "keyword_reply": {
        "name": "关键词回复规则",
        "hint": "命中任意关键词时，依次发送下方配置的所有回复（支持 [图片] 图文混排）。",
        "display_item": "remark",
        "items": {
          "remark": {
            "description": "规则备注（仅用于列表区分，不影响匹配）",
            "type": "string",
            "default": ""
          },
          "keywords": {
            "description": "关键词（每行一个，命中任意一个即触发；支持在句子中间匹配）",
            "type": "text",
            "default": ""
          },
          "reply": {
            "description": "回复内容（支持多行；用单独一行 --- 分隔多条回复；行首写 [图片] 加图片链接或本地路径可发送图片，图文位置可任意混排）",
            "type": "text",
            "default": ""
          },
          "images": {
            "description": "规则图片池（可选）：上传图片后在回复内容中用 [图片:编号] 或 [图片:文件名] 引用",
            "type": "file",
            "file_types": ["jpg", "jpeg", "png", "gif", "webp", "bmp"],
            "hint": "点击上传图片（支持 GIF 动图）。上传后在「回复内容」中写 [图片:1]、[图片:2] 按顺序引用，或 [图片:文件名.gif] 按文件名引用，即可精确控制每张图的发送时机；同一目录的图片可被多条规则复用。",
            "default": []
          }
        }
      }
    }
  }
}
